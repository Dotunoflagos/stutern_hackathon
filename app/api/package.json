{
  "name": "ezkey",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "app": "nodemon --ext js,ejs,css app.js",
    "browser-sync": "browser-sync start --proxy localhost:5000 --files views/**/*.ejs public/**/*",
    "dev": "concurrently \"npm run start\" \"npm run browser-sync\""
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
